user : "{{ lookup('amazon.aws.aws_ssm', '/roboshop/{{ env }}/rabbitmq/user', region='us-east-1', decrypt=True) }}"
password: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/{{ env }}/rabbitmq/password', region='us-east-1', decrypt=True) }}"